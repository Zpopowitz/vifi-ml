# Pinned for reproducibility. Bump versions deliberately + re-run tests.
# Test deps live in requirements-dev.txt (not audited at runtime).
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
xgboost==3.2.0
fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
httpx==0.28.1
# pandas remains for some tools (eval_harness etc.); streamlit was
# removed when the dashboard moved from Streamlit to a static SPA
# (served by FastAPI). One fewer dep, no streamlit XSS advisory.
pandas==3.0.4
joblib==1.5.3
# Audit log encryption (HIPAA: PHI at rest must be protected). Required
# in production; optional in dev (audit log falls back to plaintext
# with a startup warning).
# Bumped 43.0.1 → 46.0.7 (clears all three GHSA advisories).
cryptography==49.0.0
# Optional Prometheus metrics — only loaded when VIFI_METRICS_ENABLED=true.
prometheus-client==0.25.0
# Live message bus (Redis Streams). Optional: in-memory backend works
# without it, but production deployments and the Live dashboard need Redis.
redis==8.0.1        ; extra == "bus"
# Hardware loggers (optional; only needed when capturing real data).
bleak==0.22.3       ; extra == "capture"
godirect==1.2.1     ; extra == "capture"
# Radar raw-ADC capture over FTDI SPI. Required on the Pi for the live
# radar collector (--source ftdi) and the bench capture tooling.
pyftdi==0.57.2      ; extra == "capture"
# Software NRST through the XDS110 CMSIS-DAP probe (radar bring-up:
# tools/radar_bringup.sh + tools/capture.py reset_board).
pyocd==0.44.1       ; extra == "capture"
